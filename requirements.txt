# KosDB - Python Dependencies
# Install in order: leveldb -> plyvel -> KosDB

# Core dependencies (install plyvel_for_KosDB first)
# See: https://github.com/m5it/plyvel_for_KosDB

# Build dependencies
cython>=0.29.0  # Required to generate plyvel C++ source from _plyvel.pyx

# Additional KosDB dependencies
cryptography>=3.0.0
# uvloop>=0.15.0  # For better async performance (Linux only)

# Development dependencies (uncomment for development)
# pytest>=6.0.0
# pytest-cov>=2.10.0
# black>=21.0
# flake8>=3.9.0
