\"\"\"\nShard Manager for KosDB\n\nManages shard topology, adding/removing shards, rebalancing data,\nand tracking shard health for distributed KosDB clusters.\n\"\"\"\n\nimport json\nimport hashlib\nimport threading\nimport time\nfrom typing import Dict, Any, List, Optional, Tuple\nfrom dataclasses import dataclass, field, asdict\nfrom enum import Enum\n\n\nclass ShardRole(Enum):\n    PRIMARY = \"primary\"\n    REPLICA = \"replica\"\n    READ_REPLICA = \"read_replica\"\n\n\nclass ShardStatus(Enum):\n    ACTIVE = \"active\"\n    INACTIVE = \"inactive\"\n    DEGRADED = \"degraded\"\n    OFFLINE = \"offline\"\n\n\n@dataclass\nclass Shard:\n    shard_id: str\n    region: str\n    host: str\n    port: int\n    role: ShardRole = ShardRole.PRIMARY\n    status: ShardStatus = ShardStatus.ACTIVE\n    weight: int = 1\n    key_ranges: List[Tuple[str, str]] = field(default_factory=list)\n    last_heartbeat: float = field(default_factory=time.time)\n    metrics: Dict[str, Any] = field(default_factory=dict)\n\n    def to_dict(self) -> Dict[str, Any]:\n        return {\n            \"shard_id\": self.shard_id,\n            \"region\": self.region,\n            \"host\": self.host,\n            \"port\": self.port,\n            \"role\": self.role.value,\n            \"status\": self.status.value,\n            \"weight\": self.weight,\n            \"key_ranges\": self.key_ranges,\n            \"last_heartbeat\": self.last_heartbeat,\n            \"metrics\": self.metrics,\n        }\n\n    @classmethod\n    def from_dict(cls, data: Dict[str, Any]) -> \"Shard\":\n        return cls(\n            shard_id=data[\"shard_id\"],\n            region=data[\"region\"],\n            host=data[\"host\"],\n            port=data[\"port\"],\n            role=ShardRole(data.get(\"role\", \"primary\")),\n            status=ShardStatus(data.get(\"status\", \"active\")),\n            weight=data.get(\"weight\", 1),\n            key_ranges=[tuple(r) for r in data.get(\"key_ranges\", [])],\n            last_heartbeat=data.get(\"last_heartbeat\", time.time()),\n            metrics=data.get(\"metrics\", {}),\n        )\n\n\nclass ShardManager:\n    \"\"\"\n    Manages a cluster of shards for distributed KosDB.\n\n    Features:\n    - Add/remove shards\n    - Assign key ranges (consistent hashing or explicit ranges)\n    - Rebalance data between shards\n    - Track shard health and regions\n    - Support cross-region read replicas\n    \"\"\"\n\n    def __init__(self, db, server_id: int = 1, default_region: str = \"local\"):\n        self.db = db\n        self.server_id = server_id\n        self.default_region = default_region\n        self._shards: Dict[str, Shard] = {}\n        self._lock = threading.RLock()\n        self._version = 0\n        self._topology_key = b\"_shards:topology\"\n        self._load_topology()\n\n    def _load_topology(self):\n        \"\"\"Load shard topology from system database.\"\"\"\n        try:\n            data = self.db._system_db.get(self._topology_key)\n            if data:\n                topology = json.loads(data.decode())\n                for shard_data in topology.get(\"shards\", []):\n                    shard = Shard.from_dict(shard_data)\n                    self._shards[shard.shard_id] = shard\n                self._version = topology.get(\"version\", 0)\n        except Exception as e:\n            print(f\"[SHARD] Failed to load topology: {e}\")\n\n    def _save_topology(self):\n        \"\"\"Persist shard topology to system database.\"\"\"\n        topology = {\n            \"version\": self._version,\n            \"shards\": [s.to_dict() for s in self._shards.values()],\n        }\n        self.db._system_db.put(self._topology_key, json.dumps(topology).encode())\n\n    def create_shard(\n        self,\n        shard_id: str,\n        region: str,\n        host: str,\n        port: int,\n        role: str = \"primary\",\n        weight: int = 1,\n        key_ranges: Optional[List[Tuple[str, str]]] = None,\n    ) -> str:\n        \"\"\"Add a new shard to the cluster.\"\"\"\n        with self._lock:\n            if shard_id in self._shards:\n                return f\"ERROR: Shard '{shard_id}' already exists\"\n\n            shard = Shard(\n                shard_id=shard_id,\n                region=region or self.default_region,\n                host=host,\n                port=port,\n                role=ShardRole(role),\n                weight=weight,\n                key_ranges=key_ranges or [],\n            )\n            self._shards[shard_id] = shard\n            self._version += 1\n            self._save_topology()\n\n            # Recompute consistent hash ring if no explicit ranges\n            if not key_ranges:\n                self._recompute_hash_ring()\n\n            return f\"OK: Shard '{shard_id}' created in region '{shard.region}'\"\n\n    def drop_shard(self, shard_id: str) -> str:\n        \"\"\"Remove a shard from the cluster.\"\"\"\n        with self._lock:\n            if shard_id not in self._shards:\n                return f\"ERROR: Shard '{shard_id}' not found\"\n\n            del self._shards[shard_id]\n            self._version += 1\n            self._save_topology()\n            self._recompute_hash_ring()\n            return f\"OK: Shard '{shard_id}' removed\"\n\n    def list_shards(self) -> List[Dict[str, Any]]:\n        \"\"\"Return list of all shards as dictionaries.\"\"\"\n        with self._lock:\n            return [s.to_dict() for s in self._shards.values()]\n\n    def get_shard(self, shard_id: str) -> Optional[Shard]:\n        \"\"\"Get a shard by ID.\"\"\"\n        with self._lock:\n            return self._shards.get(shard_id)\n\n    def get_active_shards(self, role: Optional[str] = None) -> List[Shard]:\n        \"\"\"Return active shards, optionally filtered by role.\"\"\"\n        with self._lock:\n            shards = [\n                s for s in self._shards.values()\n                if s.status == ShardStatus.ACTIVE\n            ]\n            if role:\n                shards = [s for s in shards if s.role.value == role]\n            return shards\n\n    def update_shard_status(self, shard_id: str, status: str) -> str:\n        \"\"\"Update shard status.\"\"\"\n        with self._lock:\n            shard = self._shards.get(shard_id)\n            if not shard:\n                return f\"ERROR: Shard '{shard_id}' not found\"\n            shard.status = ShardStatus(status)\n            shard.last_heartbeat = time.time()\n            self._save_topology()\n            return f\"OK: Shard '{shard_id}' status updated to '{status}'\"\n\n    def heartbeat(self, shard_id: str) -> str:\n        \"\"\"Record a heartbeat from a shard.\"\"\"\n        return self.update_shard_status(shard_id, \"active\")\n\n    def _hash(self, key: str) -> str:\n        \"\"\"Return a hex hash of a key for range assignment.\"\"\"\n        return hashlib.sha256(key.encode()).hexdigest()\n\n    def _recompute_hash_ring(self):\n        \"\"\"Recompute key ranges for shards using consistent hashing.\"\"\"\n        active = self.get_active_shards(\"primary\")\n        if not active:\n            return\n\n        # Create virtual nodes based on weight\n        nodes = []\n        for shard in active:\n            for i in range(shard.weight * 10):\n                token = self._hash(f\"{shard.shard_id}:{i}\")\n                nodes.append((token, shard.shard_id))\n\n        nodes.sort(key=lambda x: x[0])\n\n        # Assign ranges\n        if len(nodes) == 0:\n            return\n\n        ranges: Dict[str, List[Tuple[str, str]]] = {s.shard_id: [] for s in active}\n        for i, (token, shard_id) in enumerate(nodes):\n            prev_token = nodes[(i - 1) % len(nodes)][0]\n            ranges[shard_id].append((prev_token, token))\n\n        for shard in active:\n            shard.key_ranges = ranges.get(shard.shard_id, [])\n\n        self._save_topology()\n\n    def rebalance_shards(self) -> str:\n        \"\"\"Recompute hash ring and return rebalance plan.\"\"\"\n        with self._lock:\n            self._recompute_hash_ring()\n            active = self.get_active_shards(\"primary\")\n            total_weight = sum(s.weight for s in active)\n            plan = []\n            for shard in active:\n                pct = (shard.weight / total_weight * 100) if total_weight else 0\n                plan.append({\n                    \"shard_id\": shard.shard_id,\n                    \"region\": shard.region,\n                    \"weight\": shard.weight,\n                    \"key_range_count\": len(shard.key_ranges),\n                    \"estimated_load_pct\": round(pct, 2),\n                })\n            self._version += 1\n            self._save_topology()\n            return f\"OK: Rebalanced {len(active)} shards\"\n\n    def get_rebalance_plan(self) -> List[Dict[str, Any]]:\n        \"\"\"Return current load distribution across shards.\"\"\"\n        with self._lock:\n            active = self.get_active_shards(\"primary\")\n            total_weight = sum(s.weight for s in active)\n            plan = []\n            for shard in active:\n                pct = (shard.weight / total_weight * 100) if total_weight else 0\n                plan.append({\n                    \"shard_id\": shard.shard_id,\n                    \"region\": shard.region,\n                    \"weight\": shard.weight,\n                    \"key_range_count\": len(shard.key_ranges),\n                    \"estimated_load_pct\": round(pct, 2),\n                })\n            return plan\n\n    def add_read_replica(\n        self,\n        shard_id: str,\n        replica_id: str,\n        region: str,\n        host: str,\n        port: int,\n    ) -> str:\n        \"\"\"Add a cross-region read replica for a primary shard.\"\"\"\n        with self._lock:\n            primary = self._shards.get(shard_id)\n            if not primary:\n                return f\"ERROR: Primary shard '{shard_id}' not found\"\n\n            if replica_id in self._shards:\n                return f\"ERROR: Shard '{replica_id}' already exists\"\n\n            replica = Shard(\n                shard_id=replica_id,\n                region=region,\n                host=host,\n                port=port,\n                role=ShardRole.READ_REPLICA,\n                key_ranges=primary.key_ranges[:],\n            )\n            self._shards[replica_id] = replica\n            self._version += 1\n            self._save_topology()\n            return f\"OK: Read replica '{replica_id}' added for shard '{shard_id}'\"\n\n    def get_topology_version(self) -> int:\n        \"\"\"Return current topology version.\"\"\"\n        with self._lock:\n            return self._version\n\n    def get_stats(self) -> Dict[str, Any]:\n        \"\"\"Return shard manager statistics.\"\"\"\n        with self._lock:\n            return {\n                \"version\": self._version,\n                \"total_shards\": len(self._shards),\n                \"active\": len(self.get_active_shards(\"primary\")),\n                \"read_replicas\": len(self.get_active_shards(\"read_replica\")),\n                \"regions\": list(set(s.region for s in self._shards.values())),\n            }\n